# Backlinks-only graph for MVP

The MVP's graph capability is limited to backlinks: showing what links to the current linkable entity. A visual graph view, structural graph queries, and an MCP server for querying the graph are all real goals for Cerebrite, but are pushed past MVP to keep the first daily-driver release scoped to what backlink navigation alone requires — a derived index that reads the markdown files, not a general query engine.
