## Agent skills

### Issue tracker

Issues live as markdown files under `.scratch/<feature-slug>/` in this repo. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.

### Running & debugging in a sandbox

To run the real Tauri app (not just the bare frontend) and debug it
interactively from inside a headless sandbox, see
`docs/agents/debugging-sandbox.md`.
